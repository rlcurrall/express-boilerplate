import { config } from 'dotenv'

/*
|--------------------------------------------------------------------------
| Load .env Config
|--------------------------------------------------------------------------
|
| Load all values from the `.env` file and put them on the `process.env'
| object.
|
*/

config()
